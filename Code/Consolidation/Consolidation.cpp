#include "Stdafx.h"

using namespace Meshmerizer;

int main(int argc, char* argv[])
{
/*
	BASIC USAGE :

 // 1) Create the builder
	MeshBuilder2* mBuilder = new MeshBuilder2;

// 2) Setup creation structure
	MBCreate Create;
	Create.NbVerts	= ...;
	...

// 3) Initialize builder
	if(!mBuilder->Init(Create))
	{
		SetIceErrorMsg("MeshBuilder init failed!");
		return false;
	}

// 4) Feed with faces
	for(udword i=0;i<Create.NbFaces;i++)
	{
		if(!mBuilder->AddFace(.....))
		{
			SetIceErrorMsg("MeshBuilder AddFace failed!");
			return false;
		}
	}

// 5) Perform mesh conversion
	MBResult MBR;

	if(!mBuilder->Build(MBR))
	{
		SetIceErrorMsg("Mesh building failed!");
		return false;
	}
*/

	return 0;
}
